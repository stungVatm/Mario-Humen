using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GlodCtr : MonoBehaviour
{
    // Start is called before the first frame update
    public int coinValue;
    void Start()
    {
        coinValue = 2;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
